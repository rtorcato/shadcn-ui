# @rtorcato/js-common — private GitLab npm registry.
@rtorcato/js-common:registry=https://gitlab.com/api/v4/projects/69645966/packages/npm/
//gitlab.com/api/v4/projects/69645966/packages/npm/:_authToken=${NPM_TOKEN}

# Everything else under @rtorcato/* falls back to the user-level private GitLab npm registry.
# NOTE: @rtorcato/js-tooling is installed via a direct tarball URL in package.json
# (public npm) because pnpm's .npmrc only supports per-scope, not per-package, routing.
@rtorcato:registry=https://gitlab.com/api/v4/packages/npm/
//gitlab.com/api/v4/packages/npm/:_authToken=${NPM_TOKEN}
